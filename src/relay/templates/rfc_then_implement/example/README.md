# Example run — `rfc-then-implement`

Worked example of running the `rfc-then-implement` template on a small
RFC: adding pagination to a public API endpoint.

The artifacts under `artifacts/` are what a real run would produce. The
`LESSONS.md` here is what `relay distill` produces from this run plus
hypothetical prior runs of the same template on the same repo.
