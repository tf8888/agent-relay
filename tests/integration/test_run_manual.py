"""Placeholder tests for `relay run` with the ManualBackend.

The ManualBackend requires interactive input (waiting for Enter or file-change
detection), so it cannot be exercised in a headless test runner.  These
scenarios are validated manually.
"""

from __future__ import annotations


def test_placeholder() -> None:
    """Manual backend tests require interactive input; tested manually."""
    pass
